# Python dependencies for the revision_2026 notebooks.
# Tested with Python 3.10+. Pin to these or newer compatible versions.
earthengine-api>=0.1.380
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.10
geopandas>=0.13
rasterio>=1.3
shapely>=2.0
pyproj>=3.5

# Optional. Only used by the hero_scatter notebook sections 8 to 11 (OLS diagnostic).
statsmodels>=0.14
